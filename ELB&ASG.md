## 高可用性与可扩展性 对于 EC2

**垂直扩展**：增加实例大小（扩大/缩小）

从：t2.nano - 0.5G 的 RAM, 1 vCPU

到：u-12tb1.metal - 12.3TB 的 RAM, 448 vCPUs

**水平扩展**：增加实例数量（扩大/缩小）

自动扩展组

负载均衡器

**高可用性**：在多个可用区运行相同应用的实例

自动扩展组多个可用区

负载均衡器多个可用区

## 可扩展性 vs 弹性 (vs 敏捷性)

可扩展性：能够通过加强硬件（向上扩展）或增加节点（向外扩展）来适应更大的负载。

弹性：一旦系统是可扩展的，弹性意味着系统可以根据负载进行“自动扩展”。这是“云友好”的：按使用付费，满足需求，优化成本。

敏捷性：（与可扩展性无关 - 分心项）新的 IT 资源只需点击一下，这意味着你将开发者从几周到几分钟内就能获取这些资源的时间缩短。

## Load Balancer:

| 特性                         | 应用程序负载均衡器（ALB）                     | 网络负载均衡器（NLB）                 | 网关负载均衡器（GWLB）                      |
|-----------------------------|--------------------------------------------|------------------------------------|--------------------------------------|
| 协议/层级                  | HTTP/HTTPS/gRPC（第7层）                   | TCP/UDP（第4层）                  | GENVE协议在IP数据包上（第3层）           |
| 主要功能和特点              | - 具有HTTP路由功能                           | - 高性能，每秒可处理数百万请求     | - 在IP数据包层级上进行流量路由              |
|                             | - 使用静态DNS（URL）                         | - 通过弹性IP提供静态IP地址         | - 将流量路由到您在EC2实例上管理的防火墙    |
|                             | - 将互联网流量合理分发到不同的服务器，       | - 适用于需要高性能和低延迟的应用程序 | - 提供入侵检测功能                         |
|                             |   确保服务器负载均衡                        |                                    |                                        |
|                             | - 根据请求类型智能路由流量至最适合处理的服务器 |                                    |                                        |


## 什么是http/https/grpc/TCP/UDP 协议/GENVE 协议

| 特性/协议 | HTTP | HTTPS | gRPC | TCP | UDP | GENEVE |
|-----------|------|-------|------|-----|-----|--------|
| **层级** | 应用层 | 应用层 | 应用层 | 传输层 | 传输层 | 数据中心网络 |
| **安全性** | 无 | SSL/TLS | SSL/TLS | 无（可靠性） | 无 | 可变 |
| **连接方式** | 无连接 | 无连接 | 长连接 | 连接导向 | 无连接 | 封装/解封装 |
| **数据传输特性** | 无状态 | 无状态 | 支持流传输 | 可靠、按顺序 | 不可靠、无序 | 高效的多协议传输 |
| **用途** | 网页数据传输 | 加密的网页数据传输 | 微服务架构中的进程间通信 | 可靠的数据传输 | 快速的数据传输，如视频流 | 网络虚拟化和多租户数据中心 |
| **协议基础** | TCP/IP | TCP/IP | HTTP/2 | IP | IP | IP |

HTTP 就像是普通的邮件服务。它把网页内容从一个地方传到另一个地方，但不提供额外的安全措施。如果有人在途中打开了信，你可能永远不会知道。

HTTPS 就是加了保险的邮件服务。它确保了信件在送达过程中不被别人窥视或更改，因为它使用特殊的锁和钥匙（加密技术），只有真正的收件人才能打开。

gRPC 是一种特殊的快递服务，它专为小包裹（或者说数据包）设计，可以更快、更有效地将信息从一个服务发送到另一个服务，还支持收件人和发件人之间的双向对话。

TCP 像是电话网络。它确保通话双方之间建立了稳定的连接，并且每个人说的话都能按照顺序并且完整地被对方听到，就算需要多说几次以确保一切都被明白。

UDP 则像是发短信。你可以快速地发送信息，但不保证对方一定会收到，也不保证短信的顺序。这种方式适合那些不介意偶尔丢失几个字，但需要快速发送的情况。

GENVE 是一种专门的邮件服务，用于处理特殊情况，比如当你要在一个大型企业内部发送很多不同类型的邮件时。GENVE能够包装这些邮件，确保它们通过企业内部复杂的邮件系统正确无误地到达正确的部门。

总之，这些协议就是定义数据如何在网络中传输的规则。HTTP和HTTPS关注的是网页内容的发送；gRPC关注的是服务之间高效的数据交换；TCP是确保数据可靠传输的通信方法；UDP是快速但不那么可靠的通信方法；而GENVE是专为网络虚拟化设计的封装协议。它们各自在互联网的不同层次上工作，确保数据可以在世界各地的计算机之间快速、安全且准确地传输。

## ASG(Auto Scaling Group)

可以想象成一个聪明的空调系统。就像空调会自动调节温度，让房间不会太热也不会太冷，AWS ASG自动调节你的服务器数量，确保你的网站或应用不会因为太多人使用而变慢或崩溃，同时也不会让你为不必要的服务器额外付费。

当你的网站或应用突然有很多人访问，比如你卖的产品突然成了热门，ASG会自动“开启更多空调”（启动更多服务器），来处理这些额外的访问量。当人们离开，流量减少时，它会“关掉多余的空调”（关闭服务器），这样你就不用为这些不再需要的服务器资源支付费用。这个过程是全自动的，就像一个保持完美温度的智能温控器，保持你的应用运行在最佳状态。

## 云中高可用性的主要目的是什么？

增加可伸缩性

即使在灾难情况下，应用程序也能正常运行

在计算机和智能手机上访问

根据需求启动EC2实例以处理更大的负载

解释：

云计算中的高可用性（High Availability, HA）指的是设计成持久且可能连续运行的系统，

目的是确保应用程序即使在出现故障或灾难情况时也能继续正常运行。高可用性通常涉及冗余和故障转移机制来最小化单点故障的风险。

## 哪种AWS提供的负载均衡器应该用来处理成千上万的连接并且低延迟？

应用程序负载均衡器

网络负载均衡器

弹性负载均衡器

解释：

网络负载均衡器（Network Load Balancer, NLB）可以处理每秒数百万个请求，并具有低延迟。它在第4层工作，非常适合负载均衡TCP、UDP和TLS流量，提供超高性能。

## 将EC2实例类型从t3a.medium改为t3a.2xlarge是以下哪种的一个例子？

水平扩展

高可用性

敏捷性

垂直扩展

解释：

从一个较小的实例类型改为更大的实例类型称为垂直扩展。这意味着你正在增加单个实例的能力，例如CPU和内存资源。

## 哪个AWS服务提供了简单的计算能力水平扩展？

EBS

AMI

IAM

ASG

解释：

自动缩放组（Auto Scaling Group, ASG）允许根据需要自动增加或减少EC2实例的数量，

提供简单的计算能力水平扩展。这有助于应对负载变化，确保应用的高可用性和成本效率。

